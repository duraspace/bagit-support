language: java
sudo: false
jdk: openjdk8

before_install:
  - "echo $JAVA_OPTS"

script:
  - mvn install -B -V
  - mvn javadoc:jar
  - mvn javadoc:test-aggregate

after_success:
  - bash <(curl -s https://codecov.io/bash)
